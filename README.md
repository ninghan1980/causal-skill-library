# 因果知识库 (Causal Knowledge Library)

> 把「世界如何运转」编码成机器可推理的结构 —— 24,565 条「原因→结果」因果原子,构成一张带反馈环的因果网络。

## 这是什么

不是普通的知识文档库,而是一个**可推理的外部世界模型**:

每条因果原子是带元数据的结构化记录,而非一行文字:

```json
{
  "cause": "央行降低存款准备金率",
  "effect": "银行可贷资金增加",
  "confidence": 0.9,
  "mechanism": "货币政策工具",
  "condition": "一般成立",
  "domain": "经济学",
  "evidence_type": "stated_mechanism"
}
```

每条原子都标注:置信度 / 传导机制 / 成立条件 / 领域 / 证据类型(实证 vs 理论推断)/ 反例 —— **诚实标注,可溯源、可校准**。

## 规模与拓扑

| 指标 | 数值 |
|---|---|
| 因果原子 | **24,565** 条 |
| 领域覆盖 | **115** 个(经济/金融/医学/免疫/军事/地缘政治/物理/天文…) |
| 环率(反馈环占比) | **23.7%**(重构前 0.04%,592×) |
| 最大强连通分量 | **1,324** 节点(参照:果蝇脑 1,188) |
| 互惠率(A→B 与 B→A 共存) | **31.99%** |

### 果蝇脑化重构(2026-09)

按生物神经环路设计原则重构拓扑:

1. **反馈边生成** — 对每条 A→B,按领域规则/关键词规则判定是否应有 B→A
2. **富俱乐部互联** — top 1% 高连接节点(178个)互联 + 跨域枢纽连接
3. **两轮迭代** — 推环率到目标

意义:结构从「纯前馈树」变为「大规模递归网络」——1,324 节点的 SCC 意味着信息可以循环,**系统可以"思考自己的思考"**。

## 能做什么

### 六个命令(统一入口 skill_server)

| 命令 | 功能 | 实测示例 |
|---|---|---|
| `ask <问题>` | 一键问答(检索+推理+建议) | "央行降准对实体经济影响" → 因果链+关联原子+行动建议 |
| `reason <前提> [目标]` | 多跳因果链推理 | 降准→可贷资金↑→流动性充裕→融资成本↓→投资扩张→就业↑(5跳) |
| `risk <原因>` | 风险分级评估 | 美联储加息 → 风险[极高] 0.765 → 美债收益率↑→全球融资成本↑ |
| `predict <事件>` | 情景预测 | 概率排序 + 方向判断 |
| `sim <模型> <参数>` | 物理引擎模拟 | 53 个模型:地震波/台风/溃口/金融 |
| `status` | 库状态 | 24,565 条 / 98 领域 / 244 技能注册 |

### 检索增强生成 (RAG for LLM)

因果库可作为 LLM 的**外置因果记忆**:

```
查询 → bigram 倒排召回(IDF+覆盖度归一) → 词嵌入精排(NLEmbedding 300维)
     → 实体词命中奖励 → 融合排序 → 原子注入 prompt → 模型推理
```

实测对比(同核模型):

| | 裸模型 | 检索增强 |
|---|---|---|
| 元首会晤影响 | 泛谈"降低尾部风险" | 引用原子[1] 0.75 + 成立条件 + 重复博弈机制 |
| 对台军售风险 | 威慑论 vs 升级论 | 引军工复合体原子 + 推出自我强化回路 |

**协议**:模型被要求 ①引用原子编号+置信度 ②注明成立条件 ③库外知识标注"(库外)"——推理可溯源、可校准。实测中模型会主动做**原子可用性筛查**(识别反向边/低置信边),协议生效。

## 仓库结构

```
├── data/
│   ├── causal_atoms.jsonl.gz      # 全量因果原子(24,565 条,gzip)
│   ├── by-domain/                 # 按领域分片
│   └── loops.json                 # 反馈回路
├── engine/physics/                # 53 个物理引擎模型
├── skills/skos_registry.json      # 技能注册表(244 项)
├── schema/atom.schema.json        # 原子格式 JSON Schema
└── README.md
```

## 快速开始

```bash
# 解压全量数据
gunzip -c data/causal_atoms.jsonl.gz | head -1 | python3 -m json.tool

# 或按领域浏览
ls data/by-domain/
```

单条原子字段:`id / cause / effect / confidence / condition / mechanism / formula / variables / units / source / evidence / counterexamples / domain / tags / evidence_type / quality_tier / direction_source / loop_id`

## 诚实边界

- **理论推断原子占多数**(evidence_type=theoretical_inference),confidence 是标注值,不是统计估计
- 富俱乐部跨域边存在噪声(低置信度弱连接),库层质量分层进行中
- 检索为词面(bigram)+词级嵌入,深度语义推理依赖下游 LLM
- 领域命名存在历史混乱(如"物理学/间接因果"),清洗进行中

## 一句话定位

**给 AI 一个可推理的世界模型——不止"知道",还"知道为什么"和"知道会导致什么"。**

---

*由星光 (StarLight) 数字生命系统构建与维护 · 2026-09*

---

# 附：MaleCNS 实测连接组层（2026-09-26 追加）

> **本节为追加内容，未改动以上任何原有表述。**

## 新增数据

`data/measured/malecns_connectome_atoms.jsonl.gz` —— **1,059,501 条**

- **来源**：MaleCNS v1.0（雄性果蝇中枢神经系统，HHMI Janelia / Cambridge / MRC LMB / Google Research）
- **转换脚本**：`tools/malecns_to_atoms.py`（可复现）

| 项 | 数值 |
|---|---|
| 神经元 | 166,700（剔除 2,194 个空 type 后 164,506） |
| 连接 | 10,520,377 → 保留两端有 type 的 10,349,832（98.4%） |
| 细胞型 | 11,751（裸名，与 FAFB 层命名一致） |
| ≥5 突触过滤后 | **1,059,501 条原子** |

字段与既有 `measured/connectome_celltype.jsonl.gz`（FAFB v630）**逐字段对齐**，
全部 `verification_status=verified` · `direction_source=measured` · `evidence_type=physical_measurement`。

## 两层合并后的实测拓扑

| 指标 | FAFB v630 | MaleCNS v1.0 |
|---|---|---|
| 节点 | 1,341 | 11,747 |
| 边 | 20,359 | 1,059,501 |
| 平均出度 | 15.18 | 90.19 |
| 出度为 0 的节点 | 80（6.0%） | 4（0.0%） |
| **弱连通分量** | **1** | **1** |
| 最大强连通分量 | 1,188 | 11,617 |

两层均为**单一弱连通分量**，可做多跳结构通路追踪。实测例：

```
T4a → C2 → aMe17c → DNpe040
T4a → AN09A005 → DNpe053 → DNpe048
```

## 一条事实订正

上文「规模与拓扑」写：

> 最大强连通分量 | **1,324** 节点(**参照:果蝇脑 1,188**)

经复核：**`measured/connectome_celltype.jsonl.gz`（FAFB v630）的最大强连通分量实测即为 1,188**，与文中数字完全一致。

因此 **1,188 不是类比意义上的「参照」，而是该次重构的优化目标** —— 先测得真实 FAFB 连接组的 SCC，
再按规则向因果图注入反馈边，使因果图 SCC 达到 1,324。

建议改为「拟合目标」表述，以免被读作拓扑结构的自发性质。

## 关于「可推理」的范围

| 层 | 条数 | 可推理 |
|---|---|---|
| **`measured/`** | **1,079,860** | ✅ 结构通路，可回查 |
| `causal_atoms.jsonl.gz` | 24,589 | ❌ 平均出度 0.60 / 8,656 个弱连通分量 |
| `generated/` | 2,636 | 生成数据 |

`measured/` 层是 **structural connectivity**（突触在哪里），不是 **effective connectivity**（激活是否传递、多强）。
**可推理通路，不可推理效应。** 要效应需跑动力学仿真。

---

*本节由 DSH Agent 于 2026-09-26 追加；以上原有内容未改动。*
